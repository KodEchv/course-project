export const ContentSection = ({ title }: { title: string }) => (
  <div className="w-full px-2 py-4">
    <span className="text-2xl text-[#d9d9d9] font-normal tracking-wide">{title}</span>
  </div>
);
